apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      component: server # note: labels (both key and val) can be arbitrary
  template:
    metadata:
      labels:
        component: server
    spec:
      containers:
        - name: server
          image: nailbiter/multi-server
          ports:
            - containerPort: 5000
